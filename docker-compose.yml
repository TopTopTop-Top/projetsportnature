services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ravitobox
      POSTGRES_PASSWORD: ravitobox
      POSTGRES_DB: ravitobox
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
